# Compile

Since we're using C++17, we have to specify it (I think if only C++17 is installed, this is not needed, but I have no idea and I do not care).

## Clang

`clang++ -std=c++17`

Note: `clang++`, not `clang`!

## GCC

`g++ -std=c++17`

## VSCode

I have no idea what is happening.

idk, sometimes there's a run button in tab bar, sometimes not

There's also a run and debug button in the bottom bar. I have no idea if that's better.
